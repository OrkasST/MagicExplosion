import { drawText, drawRect, drawLine, drawCircle } from "../utils/mainCanvasUtils.js";

export function drawOnScreen(ctx) {
  // drawRect({filled: false}, ctx);
  // drawLine({}, ctx);
  drawCircle({}, ctx);
  drawText({}, ctx);
}
